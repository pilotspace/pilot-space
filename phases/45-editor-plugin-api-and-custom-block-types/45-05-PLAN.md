---
phase: 45-editor-plugin-api-and-custom-block-types
plan: 05
type: execute
wave: 3
depends_on: ["45-02", "45-03", "45-04"]
files_modified:
  - frontend/src/features/plugins/examples/changelog/plugin.json
  - frontend/src/features/plugins/examples/changelog/index.js
  - frontend/src/features/plugins/examples/standup/plugin.json
  - frontend/src/features/plugins/examples/standup/index.js
  - frontend/src/features/plugins/examples/retro/plugin.json
  - frontend/src/features/plugins/examples/retro/index.js
  - frontend/src/features/plugins/index.ts
  - frontend/src/features/editor/layouts/EditorLayout.tsx
autonomous: false
requirements: [PLUG-06]

must_haves:
  truths:
    - "Changelog plugin generates markdown changelog from slash command /changelog"
    - "Standup plugin inserts structured standup template via /standup"
    - "Retro plugin inserts retrospective board via /retro"
    - "All three plugins demonstrate block types, slash commands, and actions"
    - "Plugin sandbox iframes load and execute in the editor context"
  artifacts:
    - path: "frontend/src/features/plugins/examples/changelog/plugin.json"
      provides: "Changelog plugin manifest"
      contains: "changelog"
    - path: "frontend/src/features/plugins/examples/standup/plugin.json"
      provides: "Standup plugin manifest"
      contains: "standup"
    - path: "frontend/src/features/plugins/examples/retro/plugin.json"
      provides: "Retro plugin manifest"
      contains: "retro"
    - path: "frontend/src/features/plugins/index.ts"
      provides: "Barrel export for plugin feature"
      exports: ["PluginSandbox", "usePluginLoader", "usePluginEditorBridge"]
  key_links:
    - from: "frontend/src/features/editor/layouts/EditorLayout.tsx"
      to: "frontend/src/features/plugins/index.ts"
      via: "import and mount"
      pattern: "usePluginEditorBridge\\|PluginSandbox"
---

<objective>
Create 3 example plugins (changelog, standup, retro) and wire the entire plugin system into the editor layout.

Purpose: Example plugins demonstrate all extension points (blocks, commands, actions) and serve as templates for teams building their own. Editor layout integration brings everything together.

Output: 3 example plugins, barrel exports, EditorLayout integration, human verification checkpoint.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-editor-plugin-api-and-custom-block-types/45-CONTEXT.md

@frontend/src/features/editor/layouts/EditorLayout.tsx
@frontend/src/features/plugins/sandbox/PluginSandbox.tsx
@frontend/src/features/plugins/hooks/usePluginLoader.ts
@frontend/src/features/plugins/integration/usePluginEditorBridge.ts
@frontend/src/features/plugins/registry/PluginRegistry.ts
@frontend/src/features/plugins/types.ts

<interfaces>
From Plan 02:
- PluginSandbox component: { manifest, jsContent, onError }
- usePluginLoader(workspaceId): { plugins, isLoading, error }

From Plan 03:
- usePluginEditorBridge(editor, monacoInstance): void

Plugin SDK (available inside iframe):
- sdk.editor.getContent() -> string
- sdk.editor.insertBlock(type, data) -> void
- sdk.editor.replaceSelection(text) -> void
- sdk.commands.register(id, handler) -> void
- sdk.actions.register(id, handler) -> void
- sdk.ui.showToast(message, type) -> void
- sdk.storage.get(key) -> value
- sdk.storage.set(key, value) -> void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 example plugins and barrel exports</name>
  <files>
    frontend/src/features/plugins/examples/changelog/plugin.json,
    frontend/src/features/plugins/examples/changelog/index.js,
    frontend/src/features/plugins/examples/standup/plugin.json,
    frontend/src/features/plugins/examples/standup/index.js,
    frontend/src/features/plugins/examples/retro/plugin.json,
    frontend/src/features/plugins/examples/retro/index.js,
    frontend/src/features/plugins/index.ts
  </files>
  <read_first>
    frontend/src/features/plugins/types.ts,
    frontend/src/features/plugins/sdk/plugin-sdk-types.ts,
    frontend/src/features/plugins/sandbox/PluginSandbox.tsx
  </read_first>
  <action>
    1. Create **Changelog plugin** (`examples/changelog/`):
       - `plugin.json`:
         ```json
         { "name": "changelog", "version": "1.0.0", "displayName": "Changelog Generator", "description": "Generates release notes from recent activity", "author": "Pilot Space", "entrypoint": "index.js", "permissions": ["editor:read", "editor:write"], "blockTypes": [{ "type": "changelog", "label": "Changelog", "icon": "FileText" }], "slashCommands": [{ "trigger": "/changelog", "label": "Insert Changelog", "description": "Generate a changelog block" }], "actions": [{ "id": "changelog.generate", "label": "Generate Changelog", "category": "note" }] }
         ```
       - `index.js`:
         - `onActivate(sdk)` registers:
           - Block type `changelog` via `sdk.editor.registerBlockRenderer`
           - Command `changelog.insert` via `sdk.commands.register` — calls `sdk.editor.insertBlock('changelog', { title: 'Changelog', date: new Date().toISOString().split('T')[0], entries: ['New feature added', 'Bug fixes', 'Performance improvements'] })`
           - Action `changelog.generate` via `sdk.actions.register` — same as command
         - Shows toast: "Changelog plugin activated"

    2. Create **Standup plugin** (`examples/standup/`):
       - `plugin.json`:
         ```json
         { "name": "standup", "version": "1.0.0", "displayName": "Daily Standup", "description": "Insert structured standup templates", "author": "Pilot Space", "entrypoint": "index.js", "permissions": ["editor:write"], "blockTypes": [{ "type": "standup", "label": "Standup", "icon": "Users" }], "slashCommands": [{ "trigger": "/standup", "label": "Insert Standup", "description": "Insert a daily standup template" }], "actions": [{ "id": "standup.insert", "label": "Insert Standup Template", "category": "note" }] }
         ```
       - `index.js`:
         - `onActivate(sdk)` registers command and action that inserts:
           `sdk.editor.insertBlock('standup', { date: new Date().toISOString().split('T')[0], yesterday: [''], today: [''], blockers: [''] })`

    3. Create **Retro plugin** (`examples/retro/`):
       - `plugin.json`:
         ```json
         { "name": "retro", "version": "1.0.0", "displayName": "Retrospective Board", "description": "Insert a retrospective board for team reviews", "author": "Pilot Space", "entrypoint": "index.js", "permissions": ["editor:write"], "blockTypes": [{ "type": "retro", "label": "Retrospective", "icon": "MessageSquare" }], "slashCommands": [{ "trigger": "/retro", "label": "Insert Retro Board", "description": "Insert a retrospective board" }], "actions": [{ "id": "retro.insert", "label": "Insert Retro Board", "category": "note" }] }
         ```
       - `index.js`:
         - `onActivate(sdk)` inserts: `sdk.editor.insertBlock('retro', { title: 'Sprint Retrospective', wentWell: [''], didntGoWell: [''], actionItems: [''] })`

    4. Create `frontend/src/features/plugins/index.ts` barrel:
       - Export: `PluginSandbox`, `usePluginLoader`, `usePluginEditorBridge`, `PluginRegistry` (all re-exports)
       - Export types: `PluginManifest`, `WorkspacePlugin`, `PluginStatus` from types
  </action>
  <acceptance_criteria>
    - grep -q "changelog" frontend/src/features/plugins/examples/changelog/plugin.json
    - grep -q "onActivate" frontend/src/features/plugins/examples/changelog/index.js
    - grep -q "standup" frontend/src/features/plugins/examples/standup/plugin.json
    - grep -q "onActivate" frontend/src/features/plugins/examples/standup/index.js
    - grep -q "retro" frontend/src/features/plugins/examples/retro/plugin.json
    - grep -q "onActivate" frontend/src/features/plugins/examples/retro/index.js
    - grep -q "PluginSandbox" frontend/src/features/plugins/index.ts
    - grep -q "usePluginEditorBridge" frontend/src/features/plugins/index.ts
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty false 2>&1 | grep "features/plugins" | head -10; echo "---"; ls frontend/src/features/plugins/examples/*/plugin.json | wc -l</automated>
  </verify>
  <done>3 example plugins exist with manifests and JS entry points, barrel exports compile</done>
</task>

<task type="auto">
  <name>Task 2: Wire plugin system into EditorLayout</name>
  <files>
    frontend/src/features/editor/layouts/EditorLayout.tsx
  </files>
  <read_first>
    frontend/src/features/editor/layouts/EditorLayout.tsx,
    frontend/src/features/plugins/index.ts,
    frontend/src/features/plugins/hooks/usePluginLoader.ts,
    frontend/src/features/plugins/integration/usePluginEditorBridge.ts,
    frontend/src/features/plugins/sandbox/PluginSandbox.tsx
  </read_first>
  <action>
    Modify `frontend/src/features/editor/layouts/EditorLayout.tsx`:
    1. Import `usePluginLoader`, `usePluginEditorBridge`, and `PluginSandbox` from `@/features/plugins`
    2. Call `usePluginEditorBridge(editor, monacoInstance)` inside the component (after editor is available)
    3. Call `usePluginLoader(workspaceId)` to get enabled plugins
    4. Render `PluginSandbox` components for each loaded plugin:
       ```tsx
       {plugins.map(p => (
         <PluginSandbox
           key={p.manifest.name}
           manifest={p.manifest}
           jsContent={p.jsContent}
           onError={(err) => console.warn(`Plugin ${p.manifest.name} error:`, err)}
         />
       ))}
       ```
    5. Place the sandbox components at the bottom of the layout (they are display:none, position doesn't matter)
    6. Guard with `workspaceId` existence check — no plugins if workspaceId not available
    7. Do NOT wrap in observer if not already — PluginSandbox and the loader are plain React
  </action>
  <acceptance_criteria>
    - grep -q "usePluginEditorBridge" frontend/src/features/editor/layouts/EditorLayout.tsx
    - grep -q "usePluginLoader" frontend/src/features/editor/layouts/EditorLayout.tsx
    - grep -q "PluginSandbox" frontend/src/features/editor/layouts/EditorLayout.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty false 2>&1 | grep "EditorLayout" | head -5; echo "---"; npx tsc --noEmit --pretty false 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>EditorLayout mounts plugin sandboxes for enabled plugins and wires the editor bridge</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end plugin system verification</name>
  <action>
    Human verifies the complete plugin system end-to-end:
    1. Plugin gallery in settings: upload, list, toggle, delete
    2. Plugin sandbox execution: iframe isolation, SDK calls, permission enforcement
    3. Editor extensibility: plugin slash commands, PM blocks, command palette actions
    4. Example plugins: changelog, standup, retro all function correctly
    5. No regression on existing editor features
  </action>
  <what-built>
    Complete editor plugin system:
    1. Plugin type contracts and backend CRUD API (upload, list, toggle, delete)
    2. Sandboxed iframe execution with permission-gated postMessage SDK
    3. Dynamic PM block types, slash commands, and actions extensible by plugins
    4. Plugin gallery in workspace settings (upload .zip, enable/disable, delete)
    5. 3 example plugins (changelog, standup, retro) demonstrating all extension points
    6. EditorLayout integration loading enabled plugins into the editor
  </what-built>
  <how-to-verify>
    1. Open workspace settings modal -> verify "Plugins" tab appears in sidebar
    2. Navigate to Plugins page -> verify empty state message
    3. Create a test plugin zip from one of the examples (zip the changelog/ folder) and upload it
    4. Verify the plugin appears in the list with name, version, author
    5. Toggle the plugin enabled/disabled -> verify status updates
    6. Open the note editor -> type "/" and verify plugin slash commands appear after built-in ones
    7. Use /changelog slash command -> verify a PM block is inserted
    8. Check the command palette (Cmd+Shift+P) -> verify plugin actions appear
    9. Delete the plugin from settings -> verify it disappears
    10. Verify existing PM blocks (decision, raci, etc.) still work correctly (no regression)
  </how-to-verify>
  <verify>Manual human verification</verify>
  <done>All 10 verification steps pass, no regressions observed</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 3 example plugins have valid manifests with blockTypes, slashCommands, actions
- EditorLayout imports and mounts plugin system
- TypeScript compilation passes
- Settings modal shows Plugins section
- Plugin slash commands appear in Monaco completion provider
</verification>

<success_criteria>
- 3 example plugins demonstrate all 3 extension points (blocks, commands, actions)
- Plugins load in sandboxed iframes inside the editor
- Full round-trip works: upload plugin -> enable -> use slash command -> see PM block
- No regression on existing editor functionality
</success_criteria>

<output>
After completion, create `.planning/phases/45-editor-plugin-api-and-custom-block-types/45-05-SUMMARY.md`
</output>
