---
phase: 46-multi-theme-system-and-editor-customization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/pilot_space/domain/workspace_member_preferences.py
  - backend/src/pilot_space/repository/workspace_member_preferences_repository.py
  - backend/src/pilot_space/services/workspace_member_preferences_service.py
  - backend/src/pilot_space/routers/workspace_member_preferences.py
  - backend/alembic/versions/062_add_workspace_member_preferences.py
  - backend/src/pilot_space/container/container.py
  - backend/src/pilot_space/main.py
  - backend/tests/test_workspace_member_preferences.py
autonomous: true
requirements: [THEME-04]

must_haves:
  truths:
    - "Backend stores per-user per-workspace theme preferences"
    - "GET /api/v1/workspaces/{slug}/preferences returns current user's theme prefs"
    - "PUT /api/v1/workspaces/{slug}/preferences upserts theme prefs"
    - "Preferences default to null fields (client applies defaults)"
  artifacts:
    - path: "backend/src/pilot_space/domain/workspace_member_preferences.py"
      provides: "SQLAlchemy model for workspace_member_preferences"
      contains: "class WorkspaceMemberPreferences"
    - path: "backend/src/pilot_space/routers/workspace_member_preferences.py"
      provides: "GET and PUT endpoints"
      exports: ["router"]
    - path: "backend/alembic/versions/062_add_workspace_member_preferences.py"
      provides: "Migration creating the table"
      contains: "workspace_member_preferences"
  key_links:
    - from: "backend/src/pilot_space/routers/workspace_member_preferences.py"
      to: "backend/src/pilot_space/services/workspace_member_preferences_service.py"
      via: "DI injection"
      pattern: "Provide\\[Container"
    - from: "backend/src/pilot_space/main.py"
      to: "backend/src/pilot_space/routers/workspace_member_preferences.py"
      via: "router include"
      pattern: "include_router"
---

<objective>
Create the backend persistence layer for per-user per-workspace theme preferences: domain model, migration, repository, service, and CRUD endpoints.

Purpose: Enables theme preferences to sync across devices and persist server-side per workspace membership.
Output: workspace_member_preferences table, GET/PUT API endpoints at /api/v1/workspaces/{slug}/preferences.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-multi-theme-system-and-editor-customization/46-CONTEXT.md

@backend/src/pilot_space/domain/
@backend/src/pilot_space/repository/
@backend/src/pilot_space/services/
@backend/src/pilot_space/routers/
@backend/src/pilot_space/container/container.py
@backend/src/pilot_space/main.py
@backend/src/pilot_space/dependencies/auth.py

<interfaces>
<!-- Follow existing patterns from workspace domain -->

Pattern from existing routers (e.g., workspace routers):
```python
# Route uses "" not "/" to avoid 307 redirects (Gotcha #1 in CLAUDE.md)
@router.get("")
async def get_preferences(
    session: SessionDep,  # REQUIRED for ContextVar (Gotcha #1)
    workspace: WorkspaceDep,
    ...
)
```

Pattern from container.py:
```python
# Add to wiring_config.modules list (Gotcha #2 in CLAUDE.md)
# Add provider: workspace_member_preferences_service = providers.Factory(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain model and migration</name>
  <files>
    backend/src/pilot_space/domain/workspace_member_preferences.py,
    backend/alembic/versions/062_add_workspace_member_preferences.py
  </files>
  <read_first>
    backend/src/pilot_space/domain/workspace.py,
    backend/src/pilot_space/domain/workspace_member.py,
    backend/alembic/versions/
  </read_first>
  <behavior>
    - Test: WorkspaceMemberPreferences model has workspace_id, user_id, theme_mode, accent_color, editor_theme_id, font_size, font_family columns
    - Test: (workspace_id, user_id) forms a unique constraint
    - Test: All theme fields are nullable (client applies defaults)
  </behavior>
  <action>
    Create `backend/src/pilot_space/domain/workspace_member_preferences.py`:
    - SQLAlchemy model `WorkspaceMemberPreferences` with:
      - `id: Mapped[uuid.UUID]` primary key (uuid4 default)
      - `workspace_id: Mapped[uuid.UUID]` FK to workspaces.id, not null
      - `user_id: Mapped[uuid.UUID]` FK to auth.users(id), not null
      - `theme_mode: Mapped[str | None]` — nullable, values: 'light', 'dark', 'high-contrast', 'system'
      - `accent_color: Mapped[str | None]` — nullable, one of 8 preset names
      - `editor_theme_id: Mapped[str | None]` — nullable, custom theme identifier
      - `font_size: Mapped[int | None]` — nullable, default null (client default 14)
      - `font_family: Mapped[str | None]` — nullable, default null (client default 'default')
      - `created_at`, `updated_at` timestamps
      - UniqueConstraint on (workspace_id, user_id)
    - Follow existing domain model patterns (check workspace_member.py for FK pattern)

    Create Alembic migration `062_add_workspace_member_preferences.py`:
    - Check current alembic heads first: `ls backend/alembic/versions/ | tail -5` to find correct parent revision
    - Create table `workspace_member_preferences` with all columns above
    - Add unique index on (workspace_id, user_id)
    - Enable RLS: `op.execute("ALTER TABLE workspace_member_preferences ENABLE ROW LEVEL SECURITY")`
    - Add RLS policy: members can read/write own row (user_id = auth.uid())
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/backend && uv run alembic heads && uv run alembic check</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class WorkspaceMemberPreferences" backend/src/pilot_space/domain/workspace_member_preferences.py
    - grep -q "workspace_member_preferences" backend/alembic/versions/062_add_workspace_member_preferences.py
    - grep -q "UniqueConstraint\|unique=True" backend/src/pilot_space/domain/workspace_member_preferences.py
    - grep -q "theme_mode" backend/src/pilot_space/domain/workspace_member_preferences.py
    - grep -q "accent_color" backend/src/pilot_space/domain/workspace_member_preferences.py
  </acceptance_criteria>
  <done>WorkspaceMemberPreferences SQLAlchemy model and Alembic migration exist, alembic check passes with single head</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository, service, router, and DI wiring</name>
  <files>
    backend/src/pilot_space/repository/workspace_member_preferences_repository.py,
    backend/src/pilot_space/services/workspace_member_preferences_service.py,
    backend/src/pilot_space/routers/workspace_member_preferences.py,
    backend/src/pilot_space/container/container.py,
    backend/src/pilot_space/main.py,
    backend/tests/test_workspace_member_preferences.py
  </files>
  <read_first>
    backend/src/pilot_space/repository/workspace_repository.py,
    backend/src/pilot_space/services/workspace_service.py,
    backend/src/pilot_space/routers/workspaces.py,
    backend/src/pilot_space/container/container.py,
    backend/src/pilot_space/main.py,
    backend/src/pilot_space/dependencies/auth.py
  </read_first>
  <behavior>
    - Test: GET /api/v1/workspaces/{slug}/preferences returns 200 with preference fields (or defaults when none saved)
    - Test: PUT /api/v1/workspaces/{slug}/preferences with {"theme_mode": "dark", "accent_color": "blue"} returns 200 with updated prefs
    - Test: PUT upserts — second PUT updates existing row, not creates duplicate
    - Test: GET returns null fields for unsaved preferences (not 404)
  </behavior>
  <action>
    Create `backend/src/pilot_space/repository/workspace_member_preferences_repository.py`:
    - `get_by_workspace_and_user(workspace_id: UUID, user_id: UUID) -> WorkspaceMemberPreferences | None`
    - `upsert(workspace_id: UUID, user_id: UUID, data: dict) -> WorkspaceMemberPreferences`
    - Follow existing repository pattern (check workspace_repository.py)

    Create `backend/src/pilot_space/services/workspace_member_preferences_service.py`:
    - `get_preferences(workspace_id: UUID, user_id: UUID) -> dict` — returns prefs dict or empty dict with null fields
    - `update_preferences(workspace_id: UUID, user_id: UUID, data: dict) -> dict` — upserts and returns updated

    Create `backend/src/pilot_space/routers/workspace_member_preferences.py`:
    - `router = APIRouter(prefix="/workspaces/{workspace_slug}/preferences", tags=["preferences"])`
    - `GET ""` — requires auth (SessionDep + WorkspaceDep pattern), returns current user's prefs
    - `PUT ""` — requires auth, accepts Pydantic schema with optional fields, upserts
    - Pydantic schemas:
      - `PreferencesResponse(theme_mode: str | None, accent_color: str | None, editor_theme_id: str | None, font_size: int | None, font_family: str | None)`
      - `PreferencesUpdate` — same fields, all optional
    - Use `""` not `"/"` for routes (Gotcha from CLAUDE.md)

    Wire DI in `backend/src/pilot_space/container/container.py`:
    - Add repository and service providers
    - Add router module to wiring_config.modules

    Mount router in `backend/src/pilot_space/main.py`:
    - `app.include_router(preferences_router, prefix="/api/v1")`

    Write tests in `backend/tests/test_workspace_member_preferences.py`:
    - Test GET returns empty/default prefs for new user
    - Test PUT creates preferences
    - Test PUT updates existing preferences (upsert)
    - Test GET after PUT returns saved values
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/.claude/worktrees/space3/backend && uv run pytest tests/test_workspace_member_preferences.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class WorkspaceMemberPreferencesRepository" backend/src/pilot_space/repository/workspace_member_preferences_repository.py
    - grep -q "class WorkspaceMemberPreferencesService" backend/src/pilot_space/services/workspace_member_preferences_service.py
    - grep -q 'router = APIRouter' backend/src/pilot_space/routers/workspace_member_preferences.py
    - grep -q "workspace_member_preferences" backend/src/pilot_space/container/container.py
    - grep -q "preferences" backend/src/pilot_space/main.py
  </acceptance_criteria>
  <done>Backend CRUD endpoints for workspace member preferences work end-to-end, DI wired, router mounted, tests pass</done>
</task>

</tasks>

<verification>
- `cd backend && uv run pytest tests/test_workspace_member_preferences.py -x` — all tests pass
- `cd backend && uv run alembic check` — single head, models match
- `cd backend && uv run ruff check` — no lint errors
- `cd backend && uv run pyright` — no type errors
</verification>

<success_criteria>
- workspace_member_preferences table with proper schema and RLS
- GET /api/v1/workspaces/{slug}/preferences returns user's prefs
- PUT /api/v1/workspaces/{slug}/preferences upserts prefs
- All fields nullable (client provides defaults)
- DI wired, router mounted
- Backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/46-multi-theme-system-and-editor-customization/46-02-SUMMARY.md`
</output>
