---
phase: 41-office-suite-preview-redesign
plan: 06
type: execute
wave: 4
depends_on: ["41-02", "41-03", "41-04", "41-05"]
files_modified: []
autonomous: false
requirements:
  - XLSX-RENDER
  - DOCX-RENDER
  - PPTX-RENDER
  - ANNOT-PANEL
  - RESPONSIVE
  - KEYBOARD

must_haves:
  truths:
    - "All six success criteria from roadmap are visually confirmed"
    - "XLSX frozen headers, search, sheet tabs, column resize work"
    - "DOCX renders in sandboxed iframe with ToC navigation"
    - "PPTX renders slides with keyboard nav, fullscreen, thumbnails, annotations"
    - "All previews respond correctly to maximize/restore"
    - "Keyboard navigation works seamlessly"
  artifacts: []
  key_links: []
---

<objective>
Run full quality gates and verify all six success criteria visually. Human confirms the Office Suite Preview redesign is complete.

Purpose: Final gatekeeper before marking phase complete — automated tests verify code, human verifies UX.
Output: Phase 41 verified and complete.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-office-suite-preview-redesign/41-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gates</name>
  <files></files>
  <read_first>
    .planning/phases/41-office-suite-preview-redesign/41-VALIDATION.md
  </read_first>
  <action>
Run all quality gates to ensure no regressions:

```bash
make quality-gates-frontend
make quality-gates-backend
```

Fix any lint, type, or test failures before proceeding to human verification.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space && make quality-gates-frontend && make quality-gates-backend</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gates-frontend` exits 0 (eslint + tsc + vitest)
    - `make quality-gates-backend` exits 0 (pyright + ruff + pytest --cov)
  </acceptance_criteria>
  <done>All quality gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Office Suite Preview redesign — XLSX, DOCX, PPTX renderers with Google Docs-level UX:
    - XLSX: frozen headers, search, column resize, sheet tabs, 500-row truncation
    - DOCX: sandboxed iframe, dual-engine render, ToC sidebar with heading navigation
    - PPTX: canvas slides, keyboard nav, fullscreen slideshow, thumbnail strip, annotation panel
    - Backend: annotation CRUD with RLS
    - All icon-only buttons have tooltips
    - Modal maximize/restore with smooth transitions
  </what-built>
  <how-to-verify>
    **Prerequisites:** Start dev servers (`cd backend && uv run uvicorn pilot_space.main:app --reload --port 8000` and `cd frontend && pnpm dev`). Log in as test user.

    **1. XLSX Preview (SC-1):**
    - Upload a multi-sheet .xlsx file via project artifacts
    - Open preview — verify frozen header stays fixed while scrolling
    - Type in search bar — verify matches highlight with green marks
    - Drag a column header edge — verify column resizes
    - Click different sheet tabs — verify data changes
    - Upload a large file (>500 rows) — verify amber truncation banner appears

    **2. DOCX Preview (SC-2):**
    - Upload a .docx file with headings
    - Open preview — verify clean prose rendering in page-like container
    - Click ToC toggle in header — verify sidebar appears with heading hierarchy
    - Click a heading in ToC — verify smooth scroll + blue highlight for 2 seconds
    - Upload a simple .docx — verify mammoth fallback works if docx-preview fails

    **3. PPTX Preview (SC-3):**
    - Upload a .pptx file with multiple slides
    - Open preview — verify slide renders on canvas with 16:9 aspect
    - Press Left/Right arrow keys — verify slide navigation
    - Click Prev/Next buttons — verify they work and disable at bounds
    - Toggle thumbnail strip — verify sidebar with slide numbers
    - Click a thumbnail — verify slide changes
    - Click fullscreen button (or press F) — verify fullscreen with dark background
    - In fullscreen, verify floating nav pill at bottom
    - Press Escape or X to exit fullscreen

    **4. Annotations (SC-4):**
    - In PPTX preview, type a note and click "Add Note" — verify it appears
    - Press Cmd+Enter to add another note — verify shortcut works
    - Edit the note (pencil icon) — verify inline edit with Save/Cancel
    - Delete a note (trash icon) — verify immediate removal with fade
    - Navigate to another slide — verify different annotation set
    - Collapse annotation panel — verify count badge on icon

    **5. Responsive (SC-5):**
    - Click maximize button — verify modal fills viewport
    - Click restore button — verify modal returns to normal size
    - Verify all three renderers adapt to both sizes

    **6. Keyboard (SC-6):**
    - Press Escape — verify modal closes
    - In PPTX, press Left/Right for slide nav
    - In thumbnail strip, press Up/Down for thumbnail nav
    - Verify tooltips show keyboard shortcuts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All six roadmap success criteria confirmed by human visual inspection.
</verification>

<success_criteria>
1. All quality gates pass (frontend + backend)
2. Human confirms all six success criteria from the roadmap
3. No visual regressions in existing preview types (code, CSV, markdown, image, HTML)
</success_criteria>

<output>
After completion, create `.planning/phases/41-office-suite-preview-redesign/41-06-SUMMARY.md`
</output>
