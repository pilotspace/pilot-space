---
phase: 05-operational-readiness
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - docs/deployment/docker-compose.md
autonomous: true
requirements:
  - OPS-01

must_haves:
  truths:
    - "docker compose config --quiet succeeds on the root docker-compose.yml (valid YAML, no syntax errors)"
    - "A developer can run 'cp .env.example .env && docker compose up -d' on a fresh Linux machine to bring up all services"
    - "The --profile production flag adds an nginx reverse proxy service"
    - "All services (postgres, redis, meilisearch, backend, frontend, supabase auth) are defined in the single root file"
    - "docs/deployment/docker-compose.md provides step-by-step guide with code blocks for every command"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Single root-level Docker Compose file with all services"
      contains: "services: postgres, redis, meilisearch, supabase-auth, backend, frontend, nginx (production profile)"
    - path: ".env.example"
      provides: "All required environment variables for single-command setup"
    - path: "docs/deployment/docker-compose.md"
      provides: "Docker Compose deployment guide"
  key_links:
    - from: "docker-compose.yml"
      to: "infra/supabase/docker-compose.yml"
      via: "Supabase services merged directly into root file (NOT include: directive)"
      pattern: "supabase.*services.*inline"
---

<objective>
Consolidate all Docker Compose services into a single root-level docker-compose.yml for one-command deployment.

Purpose: The goal is `cp .env.example .env && docker compose up -d` on a fresh Linux machine. Currently services are split across root (redis + meilisearch only), infra/docker/ (postgres + backend + frontend), and infra/supabase/ (auth services). This plan merges everything.
Output: Single root docker-compose.yml, updated .env.example, deployment guide.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operational-readiness/05-CONTEXT.md
@.planning/phases/05-operational-readiness/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build consolidated root docker-compose.yml</name>
  <files>
    docker-compose.yml,
    .env.example
  </files>
  <action>
    Read the following files first before writing:
    - /Users/tindang/workspaces/tind-repo/pilot-space/docker-compose.yml (current root — redis + meilisearch only)
    - /Users/tindang/workspaces/tind-repo/pilot-space/infra/docker/docker-compose.yml (comprehensive — all services)
    - /Users/tindang/workspaces/tind-repo/pilot-space/infra/supabase/docker-compose.yml (Supabase auth services)
    - /Users/tindang/workspaces/tind-repo/pilot-space/backend/.env.example (backend env vars)
    - /Users/tindang/workspaces/tind-repo/pilot-space/frontend/.env.example (frontend env vars)

    Overwrite the root docker-compose.yml with a consolidated version that:

    1. Uses `name: pilot-space` at the top (Compose v2 project name).
    2. Merges ALL services from infra/docker/docker-compose.yml: postgres, redis, meilisearch, backend, frontend.
    3. Merges core Supabase auth services from infra/supabase/docker-compose.yml INLINE — do NOT use `include:`. Include only services needed for auth: supabase-auth (GoTrue) and supabase-kong (API gateway). Skip supabase-studio (it's optional, add under --profile dev).
    4. Adds an `nginx` service under `profiles: [production]` for reverse proxy + SSL termination. Use `nginx:alpine` image. Mount `./infra/nginx/nginx.conf` config (create a placeholder path — the actual nginx.conf content is Claude's discretion per CONTEXT.md). Expose ports 80 and 443.
    5. All services join a `pilot-space-network` bridge network.
    6. All secrets come from environment variables (no hardcoded credentials).
    7. Healthcheck depends_on chains: backend depends_on postgres + redis (condition: service_healthy), frontend depends_on backend (condition: service_healthy).
    8. backend and frontend services use `image` from env var (e.g., `${BACKEND_IMAGE:-pilot-space/backend:latest}`) so CI can override.

    Do NOT use Compose v1 `version:` key — Compose v2 does not require it.

    Overwrite .env.example to include ALL required variables in sections:
    - # === Database === (POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_PORT)
    - # === Redis === (REDIS_URL)
    - # === Supabase Auth === (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_KEY, SUPABASE_JWT_SECRET, GOTRUE_SITE_URL, GOTRUE_SMTP_HOST, etc.)
    - # === Application === (SECRET_KEY, APP_ENV, LOG_LEVEL)
    - # === AI / BYOK === (ANTHROPIC_API_KEY — optional; OPENAI_API_KEY — optional; note: BYOK enforced at workspace level)
    - # === Meilisearch === (MEILI_MASTER_KEY)
    - # === Images === (BACKEND_IMAGE, FRONTEND_IMAGE — for CI override)

    Preserve comments explaining each variable.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space && docker compose -f docker-compose.yml config --quiet 2>&1 | head -20</automated>
  </verify>
  <done>docker compose config --quiet exits 0. Root docker-compose.yml has all services including nginx (production profile). .env.example has all required env var sections.</done>
</task>

<task type="auto">
  <name>Task 2: Write Docker Compose deployment guide</name>
  <files>
    docs/deployment/docker-compose.md
  </files>
  <action>
    Create docs/deployment/ directory if it doesn't exist.

    Write docs/deployment/docker-compose.md as a developer-facing guide (tone: "first time on a fresh machine"). Structure:

    ```markdown
    # Docker Compose Deployment

    ## Prerequisites
    - Docker 24+ and Docker Compose v2 (ships with Docker Desktop 4.x+)
    - 8 GB RAM minimum (16 GB recommended)
    - 20 GB disk space
    - Linux or macOS (Windows via WSL2)

    ## Quick Start
    [code blocks for: git clone, cp .env.example .env, edit .env (note required vars), docker compose up -d]

    ## Service Map
    [table: service name | port | purpose]
    postgres | 5432 | Primary database (pgvector enabled)
    redis | 6379 | Cache + session storage
    meilisearch | 7700 | Full-text search
    supabase-auth | 9999 | Auth service (GoTrue)
    supabase-kong | 18000 | Supabase API gateway
    backend | 8000 | FastAPI application
    frontend | 3000 | Next.js application

    ## First-Run Initialization
    [code blocks for: alembic upgrade head, seed data if any]
    - Run migrations: `docker compose exec backend alembic upgrade head`

    ## Health Verification
    [code blocks for: curl /health/live, curl /health/ready, check output]

    ## Production Deployment
    [code block: docker compose --profile production up -d]
    Notes: nginx handles SSL termination. Provide TLS certs at infra/nginx/certs/.

    ## Environment Variables Reference
    [table or description of key variables]

    ## Upgrading
    [reference to docs/operations/upgrade-guide.md]

    ## Troubleshooting
    [3-5 common issues: port conflicts, DB not ready, auth issues]
    ```

    Keep every command in a fenced code block. Do not assume prior Docker knowledge beyond "install Docker".
  </action>
  <verify>
    <automated>test -f /Users/tindang/workspaces/tind-repo/pilot-space/docs/deployment/docker-compose.md && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>docs/deployment/docker-compose.md exists with prerequisites, quick start, service map, first-run init, health verification, and production sections. Every command is in a code block.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/tindang/workspaces/tind-repo/pilot-space && docker compose -f docker-compose.yml config --quiet
test -f docs/deployment/docker-compose.md
```
</verification>

<success_criteria>
- docker compose config --quiet exits 0 (valid YAML)
- Root docker-compose.yml contains postgres, redis, meilisearch, supabase-auth, backend, frontend services
- nginx service exists under profiles: [production]
- .env.example documents all required variables in labeled sections
- docs/deployment/docker-compose.md provides step-by-step guide for a fresh Linux machine
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-readiness/05-03-SUMMARY.md`
</output>
