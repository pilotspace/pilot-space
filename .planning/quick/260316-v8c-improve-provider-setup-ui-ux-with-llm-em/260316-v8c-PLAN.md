---
phase: quick
plan: 260316-v8c
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/settings/pages/ai-settings-page.tsx
  - frontend/src/features/settings/components/provider-section.tsx
  - frontend/src/features/settings/components/provider-config-form.tsx
  - frontend/src/features/settings/components/provider-row.tsx
autonomous: true
requirements: [provider-dropdown-ui, default-embedding-model, validate-ai-features]

must_haves:
  truths:
    - "Each service section (Embedding, LLM) shows a dropdown to select the active provider"
    - "Selecting a provider shows its configuration fields inline below the dropdown"
    - "Ollama embedding defaults to nomic-embed-text-v2-moe as model name placeholder"
    - "Saving provider config works and refreshes status correctly"
    - "Status badges (Connected/Not configured/Failed) display next to the dropdown"
  artifacts:
    - path: "frontend/src/features/settings/components/provider-section.tsx"
      provides: "Dropdown-based provider section component"
    - path: "frontend/src/features/settings/components/provider-config-form.tsx"
      provides: "Provider configuration form (fields + save)"
    - path: "frontend/src/features/settings/pages/ai-settings-page.tsx"
      provides: "Updated page using new ProviderSection components"
  key_links:
    - from: "provider-section.tsx"
      to: "provider-config-form.tsx"
      via: "renders form for selected provider"
      pattern: "selectedProvider.*ProviderConfigForm"
    - from: "provider-config-form.tsx"
      to: "AISettingsStore"
      via: "saveSettings call"
      pattern: "settings\\.saveSettings"
---

<objective>
Redesign the AI Provider settings UI from expandable accordion rows to a dropdown-based approach.
Each service section (Embedding/LLM) gets a Select dropdown to pick the active provider, with
config fields shown inline below. Ollama embedding defaults to `nomic-embed-text-v2-moe`.

Purpose: Simpler, cleaner UX - user picks provider from dropdown instead of expanding multiple cards.
Output: Refactored AI settings page with dropdown provider selection.
</objective>

<execution_context>
@/Users/tindang/workspaces/tind-repo/pilot-space/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/workspaces/tind-repo/pilot-space/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/features/settings/pages/ai-settings-page.tsx
@frontend/src/features/settings/components/provider-row.tsx
@frontend/src/features/settings/components/api-key-input.tsx
@frontend/src/features/settings/components/ai-feature-toggles.tsx
@frontend/src/stores/ai/AISettingsStore.ts
@frontend/src/services/api/ai.ts
@frontend/src/components/ui/select.tsx
@backend/src/pilot_space/ai/providers/constants.py

<interfaces>
<!-- Existing types the executor needs -->

From frontend/src/services/api/ai.ts:
```typescript
export interface WorkspaceAISettingsProvider {
  provider: string;
  serviceType: 'embedding' | 'llm';
  isConfigured: boolean;
  isValid: boolean | null;
  lastValidatedAt: string | null;
  baseUrl?: string | null;
  modelName?: string | null;
  supportsBoth: boolean;
}
```

From frontend/src/stores/ai/AISettingsStore.ts:
```typescript
getProvidersByService(serviceType: 'embedding' | 'llm'): WorkspaceAISettingsProvider[]
async saveSettings(data: { api_keys?: Array<{ provider: string; service_type: 'embedding' | 'llm'; api_key?: string; base_url?: string; model_name?: string }> }): Promise<void>
```

From frontend/src/components/ui/select.tsx:
```typescript
export { Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectScrollDownButton, SelectScrollUpButton, SelectSeparator, SelectTrigger, SelectValue };
```

Backend provider slots (from constants.py):
```python
PROVIDER_SERVICE_SLOTS = [
    ("google", "embedding", False),
    ("ollama", "embedding", True),
    ("anthropic", "llm", False),
    ("ollama", "llm", True),
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProviderSection and ProviderConfigForm components</name>
  <files>
    frontend/src/features/settings/components/provider-section.tsx
    frontend/src/features/settings/components/provider-config-form.tsx
  </files>
  <action>
Create two new components that replace the expandable ProviderRow pattern with a dropdown approach:

**provider-section.tsx** — `ProviderSection` component (observer):
- Props: `serviceType: 'embedding' | 'llm'`, `icon: React.ElementType`, `title: string`, `description: string`, `onSaved: () => void`
- Uses `useStore()` to get `ai.settings.getProvidersByService(serviceType)` for the provider list
- Renders a shadcn `Select` dropdown with provider options. Each `SelectItem` value is the provider name string (e.g., "google", "ollama", "anthropic")
- Display names in SelectItem: use PROVIDER_DISPLAY_NAMES map: `{ google: "Google Gemini", anthropic: "Anthropic", ollama: "Ollama" }`
- Auto-select the first configured provider, or first provider if none configured
- Next to the SelectTrigger, show a `StatusBadge` for the currently selected provider (reuse the StatusBadge logic from provider-row.tsx — extract the icon+badge pattern: Connected/green, Failed/red, Not configured/outline, Configured/secondary)
- Below the Select, render `<ProviderConfigForm>` for the selected provider
- Wrap in a Card with the icon + title header

**provider-config-form.tsx** — `ProviderConfigForm` component:
- Props: `provider: string`, `serviceType: 'embedding' | 'llm'`, `status: WorkspaceAISettingsProvider | undefined`, `onSaved: () => void`
- Contains the same field rendering logic currently in ProviderRow's CollapsibleContent: base_url, model_name, api_key fields
- Uses the same PROVIDER_CONFIG record from provider-row.tsx (copy the config object with fields, placeholders, etc.)
- KEY CHANGE for Ollama embedding default: When `provider === 'ollama' && serviceType === 'embedding'`, set `modelPlaceholder` to `'nomic-embed-text-v2-moe'`. For `provider === 'ollama' && serviceType === 'llm'`, use `'qwen2.5'` as placeholder.
- Same save logic as current ProviderRow: builds entry object, validates required fields, calls `settings.saveSettings({ api_keys: [entry] })`, shows toast on success/error
- Uses the existing `APIKeyInput` component for API key fields
- Save button at bottom right

Keep StatusBadge as a shared helper function exported from provider-section.tsx (or inline in the file). It renders the same badges as current provider-row.tsx: CheckCircle2/green for valid, XCircle/red for invalid, Circle/outline for not configured, Circle/secondary for configured-but-not-validated.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Two new components exist, type-check passes. ProviderSection renders a Select dropdown + ProviderConfigForm. ProviderConfigForm renders fields and handles save. Ollama embedding placeholder is nomic-embed-text-v2-moe.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AISettingsPage to use dropdown-based ProviderSection</name>
  <files>
    frontend/src/features/settings/pages/ai-settings-page.tsx
  </files>
  <action>
Replace the current provider rendering in ai-settings-page.tsx:

1. Remove the `ProviderRow` import. Add import for `ProviderSection` from `../components/provider-section`.

2. Replace the Embedding Service section body:
   - Remove the `embeddingProviders.map(...)` rendering of `ProviderRow` components
   - Replace with a single `<ProviderSection serviceType="embedding" icon={Database} title="Embedding Service" description="Used for semantic search, knowledge graph, and RAG. Configure one provider." onSaved={handleProviderSaved} />`
   - Remove the section wrapper `<div>` with icon/title/description since ProviderSection handles it internally via Card

3. Replace the AI LLM Service section body:
   - Remove the `llmProviders.map(...)` rendering of `ProviderRow` components
   - Replace with a single `<ProviderSection serviceType="llm" icon={BrainCircuit} title="AI LLM Service" description="Used for AI agents, ghost text, PR review, and issue extraction." onSaved={handleProviderSaved} />`

4. Remove the `embeddingProviders` and `llmProviders` const derivations (no longer needed since ProviderSection gets providers internally from the store).

5. Keep everything else: loading skeleton, error state, AIFeatureToggles, security Alert, Separator between sections.

6. Do NOT delete provider-row.tsx yet (keep for reference / potential reuse elsewhere), but it should have zero imports in the codebase after this change.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <done>AISettingsPage renders two ProviderSection components (embedding + llm) with dropdown selection instead of expandable rows. Type-check and lint pass. Provider selection, config form, and save flow work end-to-end.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero type errors
2. `cd frontend && pnpm lint` — zero lint errors
3. Manual: Navigate to /workspace/settings/ai-services, verify:
   - Embedding section shows dropdown with "Google Gemini" and "Ollama" options
   - LLM section shows dropdown with "Anthropic" and "Ollama" options
   - Selecting a provider shows its config fields below
   - Ollama in embedding section shows "nomic-embed-text-v2-moe" as model placeholder
   - Status badge shows next to dropdown for selected provider
   - Saving a provider config works (toast appears, status updates)
</verification>

<success_criteria>
- Dropdown-based provider selection replaces expandable accordion rows
- Each service section shows exactly one Select with provider options
- Config fields render inline below the dropdown for the selected provider
- Ollama embedding model placeholder is "nomic-embed-text-v2-moe"
- Save flow works identically to before (PATCH /workspaces/{id}/ai/settings)
- Type-check and lint pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/quick/260316-v8c-improve-provider-setup-ui-ux-with-llm-em/260316-v8c-SUMMARY.md`
</output>
