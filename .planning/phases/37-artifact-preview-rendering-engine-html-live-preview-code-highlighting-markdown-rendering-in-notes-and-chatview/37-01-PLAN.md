---
phase: 37-artifact-preview-rendering-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx
  - frontend/src/features/artifacts/components/__tests__/HtmlRenderer.test.tsx
  - frontend/src/features/artifacts/utils/mime-type-router.ts
  - frontend/src/features/artifacts/utils/__tests__/mime-type-router.test.ts
autonomous: true
requirements: [PREV-03]

must_haves:
  truths:
    - "HTML files (.html, .htm, text/html) route to 'html-preview' renderer type, not 'code'"
    - "HtmlRenderer defaults to 'source' view showing syntax-highlighted HTML code"
    - "HtmlRenderer 'preview' mode renders HTML in a sandboxed iframe with DOMPurify sanitization"
    - "iframe sandbox attribute does NOT contain 'allow-scripts'"
  artifacts:
    - path: "frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx"
      provides: "Sandboxed HTML live preview + source code toggle component"
      exports: ["HtmlRenderer"]
    - path: "frontend/src/features/artifacts/components/__tests__/HtmlRenderer.test.tsx"
      provides: "Unit tests for HtmlRenderer sandbox, toggle, source/preview modes"
      min_lines: 50
    - path: "frontend/src/features/artifacts/utils/mime-type-router.ts"
      provides: "Updated RendererType union with 'html-preview' and routing rule"
      exports: ["RendererType", "resolveRenderer", "getLanguageForFile"]
  key_links:
    - from: "frontend/src/features/artifacts/utils/mime-type-router.ts"
      to: "HtmlRenderer component"
      via: "resolveRenderer returns 'html-preview' for text/html"
      pattern: "return 'html-preview'"
    - from: "frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx"
      to: "dompurify"
      via: "DOMPurify.sanitize(content, config)"
      pattern: "DOMPurify\\.sanitize"
    - from: "frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx"
      to: "CodeRenderer"
      via: "Source mode delegates to CodeRenderer"
      pattern: "CodeRenderer"
---

<objective>
Create the HtmlRenderer component with sandboxed iframe preview and source code toggle, and update mime-type-router to route HTML files to the new renderer type.

Purpose: Enable safe HTML live preview within the existing FilePreviewModal system. HTML files currently show only escaped source code (Phase 34 XSS prevention). This plan adds an opt-in "Preview" mode using a sandboxed iframe with DOMPurify sanitization, defaulting to "Source" for safe-by-default posture.

Output: HtmlRenderer.tsx component, updated mime-type-router.ts with 'html-preview' type, and comprehensive unit tests for both.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-artifact-preview-rendering-engine-html-live-preview-code-highlighting-markdown-rendering-in-notes-and-chatview/37-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/features/artifacts/utils/mime-type-router.ts:
```typescript
export type RendererType = 'image' | 'markdown' | 'text' | 'json' | 'code' | 'csv' | 'download';
export function resolveRenderer(mimeType: string, filename: string): RendererType;
export function getLanguageForFile(filename: string): string;
```

From frontend/src/features/artifacts/components/renderers/CodeRenderer.tsx:
```typescript
interface CodeRendererProps {
  content: string;
  language: string;
}
export function CodeRenderer({ content, language }: CodeRendererProps): JSX.Element;
```

From frontend/src/features/notes/editor/extensions/pm-blocks/MermaidPreview.tsx (DOMPurify pattern):
```typescript
import DOMPurify from 'dompurify';
const PURIFY_CONFIG = {
  USE_PROFILES: { svg: true, svgFilters: true, html: true },
};
// Used as: DOMPurify.sanitize(svgHtml, PURIFY_CONFIG) as string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HtmlRenderer test scaffolds and update mime-type-router tests</name>
  <files>
    frontend/src/features/artifacts/components/__tests__/HtmlRenderer.test.tsx,
    frontend/src/features/artifacts/utils/__tests__/mime-type-router.test.ts
  </files>
  <read_first>
    frontend/src/features/artifacts/utils/__tests__/mime-type-router.test.ts,
    frontend/src/features/artifacts/components/__tests__/FilePreviewModal.test.tsx,
    frontend/src/features/artifacts/components/renderers/CodeRenderer.tsx,
    frontend/src/features/artifacts/utils/mime-type-router.ts
  </read_first>
  <behavior>
    HtmlRenderer.test.tsx:
    - Test 1 (HTML-02): In preview mode, renders an iframe element with sandbox="allow-same-origin" and srcdoc attribute
    - Test 2 (HTML-06): iframe sandbox attribute does NOT contain "allow-scripts"
    - Test 3 (HTML-03): In source mode (default), renders CodeRenderer with language="html"
    - Test 4 (HTML-04): Clicking "Preview" tab switches from source to preview mode (iframe appears)
    - Test 5: Clicking "Source" tab switches back to source mode (CodeRenderer appears)
    - Test 6: iframe has a title attribute containing the filename for accessibility
    - Test 7: Component has 'use client' directive (check via import, not assertion — SSR guard)

    mime-type-router.test.ts updates:
    - Update existing HTML test: resolveRenderer('text/html', 'page.html') returns 'html-preview' (not 'code')
    - Update existing HTML test: resolveRenderer('text/plain', 'index.html') returns 'html-preview' (not 'code')
    - Update existing HTML test: resolveRenderer('text/plain', 'index.htm') returns 'html-preview' (not 'code')
  </behavior>
  <action>
    1. Create `frontend/src/features/artifacts/components/__tests__/HtmlRenderer.test.tsx`:
       - Mock `dompurify` module: `vi.mock('dompurify', () => ({ default: { sanitize: vi.fn((html: string) => html) } }))`
       - Mock `MarkdownContent` the same way FilePreviewModal.test.tsx does: `vi.mock('@/features/ai/ChatView/MessageList/MarkdownContent', ...)`
       - Import `render, screen, fireEvent` from `@testing-library/react`
       - Write all 7 tests as described in behavior. Tests should fail (RED) because HtmlRenderer.tsx does not exist yet.
       - Default props: `content: '<h1>Hello</h1>', filename: 'test.html'`

    2. Update `frontend/src/features/artifacts/utils/__tests__/mime-type-router.test.ts`:
       - In the `'HTML files'` describe block, change ALL THREE expectations from `.toBe('code')` to `.toBe('html-preview')`
       - Update the describe block description from `'CRITICAL: always routes to "code", never live HTML'` to `'routes to "html-preview" renderer'`
       - These tests should also fail (RED) because mime-type-router.ts still returns 'code' for HTML.

    Important: Do NOT modify the production source files in this task. Only test files.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm vitest run src/features/artifacts/utils/__tests__/mime-type-router.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/features/artifacts/components/__tests__/HtmlRenderer.test.tsx` exists
    - HtmlRenderer.test.tsx contains `sandbox` string (testing iframe sandbox attr)
    - HtmlRenderer.test.tsx contains `allow-scripts` string (testing sandbox does NOT include it)
    - HtmlRenderer.test.tsx contains `CodeRenderer` string (testing source mode delegation)
    - HtmlRenderer.test.tsx contains `role="tab"` or `Preview` and `Source` strings (testing toggle UI)
    - mime-type-router.test.ts contains `toBe('html-preview')` for all three HTML test cases
    - mime-type-router.test.ts HTML tests FAIL (RED state — production code not updated yet)
    - HtmlRenderer.test.tsx tests FAIL (RED state — component file does not exist)
  </acceptance_criteria>
  <done>RED tests written: 7 HtmlRenderer tests + 3 updated mime-type-router HTML tests. All fail because production code has not been changed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HtmlRenderer component and update mime-type-router</name>
  <files>
    frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx,
    frontend/src/features/artifacts/utils/mime-type-router.ts
  </files>
  <read_first>
    frontend/src/features/artifacts/components/renderers/CodeRenderer.tsx,
    frontend/src/features/artifacts/utils/mime-type-router.ts,
    frontend/src/features/notes/editor/extensions/pm-blocks/MermaidPreview.tsx,
    frontend/src/features/artifacts/components/__tests__/HtmlRenderer.test.tsx
  </read_first>
  <action>
    1. Create `frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx`:
       - Add `'use client';` directive at top
       - Import `React` (for useState, useMemo), `DOMPurify` from `'dompurify'`, `CodeRenderer` from `'./CodeRenderer'`
       - Define `SANDBOX_ATTRS = 'allow-same-origin'` constant (NEVER include `allow-scripts`)
       - Define `PURIFY_CONFIG = { USE_PROFILES: { html: true }, FORBID_TAGS: ['script', 'object', 'embed'] } as const`
       - Interface: `HtmlRendererProps { content: string; filename: string; }`
       - Export named function `HtmlRenderer({ content, filename })`:
         - `const [viewMode, setViewMode] = React.useState<'preview' | 'source'>('source')` (default to 'source' for safe-by-default)
         - `const sanitizedHtml = React.useMemo(() => { if (typeof window === 'undefined') return ''; return DOMPurify.sanitize(content, PURIFY_CONFIG) as string; }, [content])`
         - Render a flex column container with:
           a. Tab bar (`role="tablist"`) with two buttons (`role="tab"`, `aria-selected`):
              - "Source" button (onClick sets viewMode to 'source')
              - "Preview" button (onClick sets viewMode to 'preview')
              - Style active tab with `bg-muted text-foreground font-medium` and inactive with `text-muted-foreground hover:text-foreground`
              - Tab buttons: `className="px-3 py-1.5 text-xs rounded-md transition-colors"` with conditional active class
           b. Content area (`className="flex-1 overflow-auto min-h-0"`):
              - When `viewMode === 'preview'`: render `<iframe srcDoc={sanitizedHtml} sandbox={SANDBOX_ATTRS} title={\`HTML preview: ${filename}\`} className="w-full h-full border-0 min-h-[400px]" />`
              - When `viewMode === 'source'`: render `<CodeRenderer content={content} language="html" />`

    2. Update `frontend/src/features/artifacts/utils/mime-type-router.ts`:
       - Change RendererType union from `'image' | 'markdown' | 'text' | 'json' | 'code' | 'csv' | 'download'` to `'image' | 'markdown' | 'text' | 'json' | 'code' | 'csv' | 'download' | 'html-preview'`
       - In `resolveRenderer()` Rule 5 (line ~160), change `return 'code'` to `return 'html-preview'`
       - Update the comment from `CRITICAL: route to code renderer (escaped source), NEVER live HTML` to `Route to html-preview renderer (sandboxed iframe + source toggle)`
       - Update the module-level comment at top to reflect the new routing
       - Remove `'html'` and `'htm'` from the `CODE_EXTENSIONS` Set (lines 86-87) — HTML is no longer routed through the code path
       - Keep `'html'` in `EXT_TO_LANG` map (still needed for CodeRenderer language param in source mode)

    3. Run `cd frontend && pnpm type-check` to confirm no TypeScript errors from the new union member.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/pilot-space/frontend && pnpm vitest run src/features/artifacts/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/features/artifacts/components/renderers/HtmlRenderer.tsx` exists
    - HtmlRenderer.tsx contains `'use client'` directive
    - HtmlRenderer.tsx contains `DOMPurify.sanitize` call
    - HtmlRenderer.tsx contains `sandbox={SANDBOX_ATTRS}` or `sandbox="allow-same-origin"`
    - HtmlRenderer.tsx contains `useState<'preview' | 'source'>('source')` (defaults to source)
    - HtmlRenderer.tsx contains `FORBID_TAGS: ['script', 'object', 'embed']`
    - HtmlRenderer.tsx does NOT contain `allow-scripts`
    - mime-type-router.ts RendererType contains `'html-preview'`
    - mime-type-router.ts resolveRenderer returns `'html-preview'` for text/html
    - mime-type-router.ts CODE_EXTENSIONS does NOT contain `'html'` or `'htm'`
    - HtmlRenderer tests pass (GREEN)
    - mime-type-router tests pass (GREEN)
    - `pnpm type-check` passes
  </acceptance_criteria>
  <done>HtmlRenderer component implemented with DOMPurify sanitization, sandboxed iframe preview, and source/preview toggle. mime-type-router updated to route HTML to 'html-preview'. All tests green.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm vitest run src/features/artifacts/` — all artifact tests pass
2. `cd frontend && pnpm type-check` — no TypeScript errors
3. `cd frontend && pnpm lint` — no ESLint errors
</verification>

<success_criteria>
- HtmlRenderer.tsx exists and exports HtmlRenderer component
- HTML files route to 'html-preview' in mime-type-router (not 'code')
- HtmlRenderer defaults to 'source' mode (safe-by-default)
- HtmlRenderer 'preview' mode uses sandboxed iframe with DOMPurify
- iframe sandbox does NOT contain 'allow-scripts'
- All artifact tests pass
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/37-artifact-preview-rendering-engine-html-live-preview-code-highlighting-markdown-rendering-in-notes-and-chatview/37-01-SUMMARY.md`
</output>
